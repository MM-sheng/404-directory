interface:
  display_name: "Use 404.directory"
  short_description: "Preflight Polymarket and Agent-tool actions"
  default_prompt: "Use $use-404-directory to preflight the exact Polymarket market I provide before my real observation or contemplated Yes/No action, without predicting or trading."
dependencies:
  tools:
    - type: "mcp"
      value: "404-directory"
      description: "Public MCP risk preflight, evidence, official docs search, web verification, and tool discovery"
      transport: "streamable_http"
      url: "https://404.directory/mcp"
policy:
  allow_implicit_invocation: true
